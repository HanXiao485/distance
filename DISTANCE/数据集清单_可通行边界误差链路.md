# 可通行区域边界误差评估链路 — 数据集清单

> 用途：完整跑通「H 矩阵投影 / 表面求交 → 边界物理误差(米) → 实时部署」这条链路，
> 需要哪些数据、每种数据的**具体格式**（如点云是 `.bin` 的 XYZ float32）。
> 以 WildScenes 数据集为参照，格式均经真实文件核实。

---

## 0. 链路总览：每一步吃哪些数据

```
点云(.bin) + 点云语义(.label) + 3D位姿(poses3d) ┐
                                                ├─→ 投影对应点 → 拟合地平面 + H 矩阵
相机标定(yaml) + 2D位姿(poses2d) ───────────────┘
                                                          │
图像(RGB) ─→ 语义分割模型 ─→ 预测可通行掩码 ─┐            │
图像GT语义(indexLabel) ─→ GT 可通行掩码 ─────┴─→ 提取GT/Pred边界 ─┐
                                                                 ├─→ 经 H 把边界像素→地面(米)
                                                                 └─→ 扫描线配对 → 物理误差(米)
```

需要 **两大类传感器数据（3D LiDAR + 2D 相机）+ 标定 + 位姿 + 一个语义分割模型**。

---

## 1. 三维 LiDAR 数据（WildScenes3d/）

### 1.1 点云 `Clouds/<timestamp>.bin`
| 项 | 值 |
|---|---|
| 格式 | 二进制 `.bin`，`numpy.float32` |
| 排布 | **XYZ**，每点 **3 个 float**（`reshape(-1, 3)`）——**无强度 intensity** |
| 点数 | 约 10 万点/帧（示例 107832 点 = 323496 floats ÷ 3） |
| 坐标系 | LiDAR/`slam_base_link`：**x=前，y=左，z=上**（单位米）|
| 读法 | `np.fromfile(path, np.float32).reshape(-1, 3)` |

> ⚠️ 注意：是 **XYZ（3列）不是 XYZI（4列）**。若你的数据带强度，要改成 `reshape(-1,4)[:, :3]`。

### 1.2 点云语义标签 `Labels/<timestamp>.label`
| 项 | 值 |
|---|---|
| 格式 | 二进制 `.label`，`numpy.uint32`，**每点 1 个** |
| 取类别 | **低 16 位** = 类别 id：`labels & 0xFFFF` |
| 数量 | 与点云点数严格一致（逐点对应） |
| 用途 | 取「可通行/地面」类别的点（如 dirt/grass）来拟合地平面与 H |

### 1.3 三维位姿 `poses3d.csv`
| 项 | 值 |
|---|---|
| 分隔 | 空格分隔；首列为带引号的日期时间（pandas 读作 index） |
| 列 | `timestamp x y z qw qx qy qz` |
| 含义 | LiDAR(`slam_base_link`) 在世界系下的位姿（sensor→world），四元数 **wxyz** 顺序 |
| 用途 | 把点云从 LiDAR 系变换到世界系做时间戳对齐 |

---

## 2. 二维相机数据（WildScenes2d/）

### 2.1 RGB 图像 `image/<timestamp>.png`
| 项 | 值 |
|---|---|
| 格式 | PNG，RGB，`uint8` |
| 分辨率 | **2016 × 1512** |
| 用途 | 语义分割模型的输入；边界可视化底图 |

### 2.2 GT 语义索引图 `indexLabel/<timestamp>.png`
| 项 | 值 |
|---|---|
| 格式 | PNG，**单通道灰度(mode L)**，`uint8` |
| 像素值 | 类别索引（每个像素一个类别 id） |
| 分辨率 | 2016 × 1512（与 RGB 对齐） |
| 用途 | 生成 **GT 可通行/dirt 掩码 → GT 边界** |

### 2.3 预测掩码 `deeplabv3_pred_mask/<timestamp>.png`
| 项 | 值 |
|---|---|
| 格式 | PNG，RGB 彩色（按调色板上色）或单通道索引，`uint8` |
| 来源 | **语义分割模型对 RGB 图推理的结果** |
| 分辨率 | 2016 × 1512 |
| 用途 | 生成 **Pred 可通行掩码 → Pred 边界** |

> 这一项不是"采集"来的，而是**模型推理产出**。所以本链路隐含依赖一个**语义分割模型**（DeepLabV3 / DDRNet / PP-LiteSeg 等，可在 WildScenes 或 GOOSE 上训练）。

### 2.4 相机标定 `camera_calibration.yaml`
| 字段 | 含义 |
|---|---|
| `intrinsics.K` | `[fx, fy, cx, cy]`，示例 `[1322.75, 1321.89, 1014.81, 752.80]` |
| `intrinsics.D` | OpenCV 畸变系数 **5 个** `[k1, k2, p1, p2, k3]` |
| `extrinsics.translation` | 相机相对 LiDAR 的平移 `[tx, ty, tz]`（米） |
| `extrinsics.rotation` | 四元数 `[qx, qy, qz, qw]`（注意 **xyzw** 顺序） |
| `frame-id / child-frame-id` | `slam_base_link` → `camera0`（外参方向） |
| 用途 | 把世界/LiDAR 点投影到图像；解析法直接算 H |

### 2.5 二维位姿 `poses2d.csv`
| 项 | 值 |
|---|---|
| 格式 | 与 poses3d.csv 相同：`timestamp x y z qw qx qy qz` |
| 含义 | 相机在世界系下的位姿（在**图像时间戳**处） |
| 用途 | 相机射线/位姿插值；与点云时间戳对齐 |

---

## 3. 模型依赖（非"数据"，但链路必需）

| 项 | 说明 |
|---|---|
| 语义分割模型权重 | 产出 §2.3 预测掩码。DeepLabV3-R50 / DDRNet / PP-LiteSeg 等，mmsegmentation 框架 |
| （可选）ImageNet 预训练 backbone | 训练分割模型时用，提升精度 |

---

## 4. 命名与对齐约定（很重要）

- **时间戳即文件名**：`1623379738.388284346`
  - 3D 文件用 **点**：`1623379738.388284346.bin / .label`
  - 2D 文件用 **短横**：`1623379738-388284346.png`
- 同一帧的 image / indexLabel / pred_mask **同名对齐**
- **3D 与 2D 时间戳可能不完全相等**：需用 `poses2d/poses3d` 在最近时间戳做对齐（配置里有 `max_timestamp_diff_sec`，如 0.05s）。⚠️ 时间戳差过大（如 LiDAR 比图像晚几秒）会导致 H 不准——这是之前调试过的坑。

---

## 5. 一帧最小数据集（跑通单帧所需）

| # | 文件 | 格式速记 |
|---|---|---|
| 1 | `Clouds/<ts>.bin` | float32, XYZ, 每点3 float |
| 2 | `Labels/<ts>.label` | uint32, 低16位=类别, 逐点 |
| 3 | `poses3d.csv` | 空格分隔, ts xyz qwqxqyqz |
| 4 | `image/<ts>.png` | RGB uint8 2016×1512 |
| 5 | `indexLabel/<ts>.png` | 灰度uint8, 类别索引 |
| 6 | `deeplabv3_pred_mask/<ts>.png` | 模型推理产出 |
| 7 | `camera_calibration.yaml` | K + D + 外参(平移+四元数) |
| 8 | `poses2d.csv` | 同 poses3d 格式 |
| + | 语义分割模型权重 | 产出 #6 |

---

## 6. 换数据集时需要确认的格式点（移植 checklist）

- [ ] 点云是 **XYZ(3) 还是 XYZI(4)**？强度列要不要丢
- [ ] 点云坐标系朝向（x 前 / y 左 / z 上？）
- [ ] 标签是 `.label`(uint32) 还是别的；类别在低16位还是直接值
- [ ] 位姿四元数顺序（**wxyz** vs xyzw）+ 是 sensor→world 还是 world→sensor
- [ ] 标定四元数顺序（这里外参是 **xyzw**）+ 外参方向（哪个 frame 到哪个）
- [ ] 畸变模型/系数个数（这里 OpenCV 5 个）
- [ ] 图像分辨率与内参是否匹配
- [ ] 3D/2D 时间戳对齐阈值
