# Python
__pycache__/
*.pyc
*.egg-info/
.env

# OS
.DS_Store

# Outputs / generated artifacts
outputs/
*.bak_preopt

# Raw data / datasets (kept locally or on the training server, not in git)
datasets/
data/
wildscenes_mini/
raw/
worst_frames/
labels/
calibration/

# Weekly reports (kept locally, not in this code repo)
周报资料/

# Model weights / checkpoints
pretrained_models
work_dirs/
vis_output/
trt_engines/
*.pth
*.pt
*.engine
*.trt
